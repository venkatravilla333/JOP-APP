import React, { useReducer, useState } from 'react'

function G() {

  // var initialState = {
  //   noOfCakes: 200
  // }

  // var reducer = (state = initialState, action) => {
  //   switch (action.type) {
  //     case 'BUYCAKE':
  //       return {
  //         noOfCakes: state.noOfCakes - 1
  //       }
  //       break;
     
  //   }
  // }
  // var [state, dispath] = useReducer(reducer, initialState)
  return (

    <div>
      {/* <h3>G: noOf cakes: {state.noOfCakes}</h3>
      <button onClick={()=>dispath({type: 'BUYCAKE'})}>buy cake</button> */}
     
    </div>
  )
}

export default G